<% provide(:title, 'Create card') %>
<h1>Create card</h1>

<div>
	<%= form_for(@card) do |f| %>
		<%= f.label :name %>
		<%= f.text_field :name %>

		<%= f.label :discreption %>
		<%= f.text_area :discreption %>

		<%= f.label :column_id %>
		<%= f.text_field :column_id %>

		<%= f.label :deadline %>
		<%= f.date_select :deadline %>

		<%= f.submit "Submit", disable_with: 'Submiting...' %>
	<% end %>
</div>